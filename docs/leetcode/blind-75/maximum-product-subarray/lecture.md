# Maximum Product Subarray

Welcome back to this video. In this video, we are going to solve this coding interview question, maximum product subarray. In this problem, we are given an array of integers. We have to find out maximum product of a contiguous subarray, and we have to return the maximum product of this given array. This subarray, this entire array contains maximum product, which is one times two. Times 2, times 4. 1 times 2 equals to 2. Times 3 times 4 equals to 12. So 2 times 12 equals to 24. So for this given array, we have to return 24. 24 is the maximum product of this contiguous subarray. We find it maximum contiguous subarray product twenty-four, so returning twenty-four. If we are given this array of integers, what is the maximum product subarray? In this array, this is the subarray. This subarray contains maximum product. This is the contiguous subarray. So minus two times minus three Equals to six times four minus four times minus five, so twenty. So here we get six times twelve, so one two zero. So for this given array of integers, we have to return one hundred twenty, because in this array, this subarray contains the Maximum product salary This is our contiguous subarray. This contiguous subarray has this product one twenty. So, I'm giving you one twenty. If we are given this array of integers, in this array of integers, this This subarray contains the maximum product, which is minus two minus three times four, which is two times minus three equals to six times four equals to You gotta hurry up. To twenty-four.

So what is given array of integers? We have written 24. If we are given this array of integers, in this array of integers, this is the continuous subarray which is containing the maximum product, which is 2 times 3 equals to 6. We have written 6 for this given array of integers. If we are given this array of integers, in this array of integers, we see that this continuous subarray contains maximum product. 3 times 5 times minus 1 times minus 2. 3 times 5 equals to 15, and minus 1 times minus 2 equals to 2, so we get 13. So for this given array of integers, we have to return 13. This is our problem. Now let's see how to solve this problem. Now let's see how to solve this problem. We are going to use


arr = [-1,-2,-3,0,3,5,-1,-2]

Use two variable minimum and maximum to solve this problem. Initially, minimum equals to, minimum equals to one and maximum equals to one. Let's create a variable answer in this variable we are going to store the maximum product subarray, the maximum product of a subarray. So initially, answer equals to the first element, which is minus one. This is the initial value of this variable answer. In this level The minimum length, the minimum length of our given array is one. The minimum length is one. So we are gonna take minus one, the first element. We're gonna take the first element as the initial value of this variable answer. So initially, answer equals to minus one. It means initially the maximum product of a subarray is minus one. 

We have value one, minimum value one and maximum value one. What it tells us? It tells us if this is our current element, this two variable tells us including the previous element of our current element. Look, this two variable mean and max, the initial value is one. What it tells us? It tells us if this is our current element This minimum and maximum value tells us, including our previous element of our current element. The minimum product of contiguous subarray is one and maximum product of contiguous subarray is one So including the previous element, this minimum max tells us including the previous element, the product of contiguous subarray is one, the minimum product of contiguous subarray is one, and the maximum product of contiguous subarray is one. Now let's compute the minimum and maximum for this, for this current element. Including minus one, we're gonna compute the minimum product of a contiguous subarray, and including minus one, we're gonna compute the maximum product of a subarray. Right. We need minimum and maximum value. Let's assume here we have a negative value and here we have a positive value. When we are processing this cell, we have to find out the maximum product, the maximum Product of the conjugate sum. So here, if we multiply this minus one with this minimum value, we'll get a positive value. If we multiply this value with this positive value, we'll get a negative value. We see that we are getting maximum value by multiplying this minus two with the minimum value that we gained, right? Including minus one. The minimum possible product of a continuous number including minus one is this negative value. If we get, we will multiply with this minus to get the maximum value. So we need the minimum value. And if we have here a positive value instead, we will multiply this to it minus this negative value, what you will get negative value, if you multiply with this, what you will get, you will get a positive value. So we get the maximum of our product for the positive value. We don't know what the next value, what the next value we have in the next minus one. We don't know, it can be negative or it can be positive. We have to handle both the cases. That's why we need here minimum and maximum. Well, let's compute the minimum product of a contiguous subarray including minus one and maximum product including minus one. But let's compute for minimum. Minimum equals two. Past this element itself, past this element itself, it can be something like this. If we have here minus ten, here we have minus, minus eight. If we multiply minus eight with minus ten, we get ten. If we multiply with minus eight, we get eight. What is the minimum? What is the minimum? Perfect. Including minus one. We see that which is minus one. So we have to consider this single element as well. So this element itself, now let's multiply this value with minimum. So what do we get? We get minus one times one. We have to multiply this value with this max value. Then what we will get? We will get minus one times one. You may ask, why we are multiplying this minus one with max? We have here minimum value, so we can multiply this, so we can multiply this value with minimum value, we will get minimum value. No, this is not the case always, it's. If we have here minus negative value. Let's assume we have here minus two, and here we have two. Plus five. So if you multiply with this value minus one and minus two, we get two. If you multiply with its max value, what we get? We get minus five. So in that case, we get the minimum value for the maximum value. We don't know by whose value we are getting the minimum value, so we have to multiply with minimum as well as with maximum. So we get The minimum value minus one. Here we have these three minus one, minimum of these three, we get minus one. So including this minus one, we get the minimum product, subbed in, the minimum product, minus one. Now let's compute max. Max equals to, max equals to this value, this value can be the maximum value, because if we have here minus ten, we have a minus ten, and if we have here one hundred instead of we have here one hundred. Then we don't get one hundred, then minus ten times one hundred Minus ten times one hundred. We said that you're getting the maximum value, this value, remember, multiplied, right? So you have to consider this as well. So this value that we have to multiply with the minimum and maximum. To get the maximum value, if we have here Minimum value. If we have here negative value, we will get the maximum value by multiply with the minimum value, right? So first this value itself, our current value, then our current value times minimum. Minimum is minus one. Here we have one actually. This is just for sake of understanding. We put here, we put here minus one. Now minus one times one. If you have a positive value, if this is a positive value, and if this is a Negative value, then we'll get the maximum value for maximum. So we have to consider minimum and maximum. We have to multiply this one with minimum as well as with maximum. So what do we get? So we have to consider minimum and maximum. So here we get the maximum value minus one. So we get the maximum of this three is minus one. So including this minus one, we get the value minus one and minus one. The minimum product is minus one, maximum product is minus one as well. Now, we computed maximum value, so let's check our answer. We see max of minus one and minus one is minus one. Now let's move forward, we have to compute for this cell. We have to compute minimum and maximum for this cell minus two. Let's compute the minimum and maximum. In order to compute minimum and maximum here, we need the value, minimum and maximum value. Minimum and maximum product we need for our previous sale, including our previous sale, including our previous limit, we know that The minimum product is minus one, maximum product is minus one. So we can find out the minimum and maximum product, including this minus two easily, right? This is kind of a self-explanatory logic. Now let's compute it. So first, minimum equals to minus two. Let's multiply this minus two with minus one. Minus two times minus one, then minus two times minus one. What is minimum? Minimum is minus two. So find out the minimum value here, minus two. Now let's compute maximum. So maximum equals to minus two is equal to itself, then minus two times minimum, which is minus one, then minus two times, minus two times minus one, we get here two. So max is two. Up until this point, we find out the maximum product, the maximum product is two. So including this current element, the maximum product is two and the minimum product will get minus two. We need minimum and maximum to calculate the product for the next cell. We already discussed it So now let's take the max of two and minus one versus two. It's arbitrary with two. Now with the help of minimum and maximum, we're gonna find out the maximum value including this minus three. Also, we're gonna find out the minimum value including this minus three for the next step. We have here minus three. If we multiply this with this minus two, what we get? Let's compute minimum, minimum equals to minus three. Let's multiply with minus two, what we get? We get minus six, three times equals to, I said this is Plus 6, then here we get, we get 2 times 2, which is minus 6. What is minimum? Minimum is minus 6. So the minimum is minus six. We need this value to compute To compute the value for the next step, if we have here a negative value, something like this, let's assume we have negative four, we have negative four In order to compute the value, the maximum product over this, we need this value, minimum value. So the negative value, we need this value, minimum value. So minus four times minus six, we get, we get twenty-four. We'll see this, let's compute maximum. Maximum equals to minus three, the minus three times Minus two versus six, ten. Minus three times two, this is minus six. So max is this, minus three times minus two, we get six. So max we get here six. So get max answer equals to six at this point. We see that including this element, the maximum product is six, minimum product is minus six. We solve this problem. After solving this subarray, we have minimum equals to minus six, max equals to six. It means that from this subarray, the minimum contiguous subarray product is minus six. And maximum subarray product is six, so minimum subarray product from this continuous array is minus six and maximum is six. So the minimum subarray product from this continuous array is minus six and the maximum subarray product of this continuous array is six. Now let's compute the value of the next cell If we have here a value versus negative value, so in order to compute minimum here, what we have to do, minimum equals to minus four, then minus four times six equals to 24, then what we get minus 24. So in order to compute the maximum value for this cell When did this minimum value? Let's see a maximum. Maximum equals to minus four, minus four times minus six, which is twenty-four, when minus four times six, which is minus twenty-four. You find it is twenty-four, that is the minimum. If it don't have this minimum, we cannot compute the maximum value. If we have a negative value, if we have a positive value for, then we get the maximum value for this maximum. So in that case, what do we get? We get four divided by four times six equals to twenty-four, then minus twenty-four. We get the maximum, we get the maximum value. This is for max. So we need minimum and maximum. We need minimum in order to compute the maximum product sum if we have in the next cell negative value. We need maximum if we have in the negative cell positive value. Now we find out a different value which is zero. If you multiply With the zero. All the balance becomes zero. So here what we'll get, we'll get directly zero here, minimum zero, maximum zero. So here we reset our minimum and maximum to zero. Now we find out it's zero, and we get the minimum equals to zero and maximum equals to zero. So we solve this sub solving this subarray, what we get from this subarray, the maximum value we get here is six, the maximum product of continuing subarray we find out six. Now we're going to reset it. Now we're gonna consider we have only this array, we're considering this is array, and our initial value we are going to change. This minimum we to one. We're gonna change this minimum to one and this max to one. We're sitting it. We're starting here. Something like we are solving this problem for another examples. We aren't curing this, but we already solved this portion. Now let's solve for this portion, which our value we reset it with, we reset our value with one one. This is your initial value, and this is your first value of our array. Now let's compute our minimum and maximum. In a variable, we'll have the answer for this operation. Now let's compute minimum. Minimum equals to Minimum of three, three times one is three, three times one is three, so minimum is three. Minimum is three and the max, max equals to three times one, and three vest and three times mean three, three times max is three, so max equals to three. Here what? Minimum is three and the max equals to three. Now let's check our, our answer, which is six, max is three, so six is greater than three. Let's move forward Is it ever current? So including three, we already sold including three, we have three, mid three and max three Look how long this thing is. You got one. Why are you whispering like I can hear you? I said, "Look how long this video is." 26 minutes? We haven't left the screen yet. I find that, oh no, sir. Don't squash, fuck that squash that zucchini up, bro. And then Jim and I was like, "They absolutely love zucchini." I said, "Also, this is their fucking candy. We gave them their candy." How would you know? I'm so angry. They're going to be beautiful

So including three, the maximum product, three minimum product is two. Now let's Third, fourth, fifth We already sold for three, including three, we have minimum and maximum. Now, by including five Including five, what is minimum? Minimum equals to five, then five times three is fifteen, then again five times three is fifteen. Here max, max equals to five fifteen fifteen So here the minimum is fine And here the maximum is That's it. So including our five, we'll find out the minimum value. Including our five, we'll find out the minimum value. This minimum value helps us handling the negative value. If you have a negative value in the next, in the next year, we'll find it max equals to fifty. Including five, the maximum product value is fifty. The max of six and fifteen is fifty. Let's go forward. This is our current So here let's compute minimum. What is minimum? Including minus one. So minimum equals to minus one minus one times five versus minus one, then minus one times fifty versus minus fifty Well, Max, what is Max including our minus one? So minus one, then minus five, then minus fifty So what is minimum? Minimum is minus fifty. So here we get value minus fifty. We use this value to handle negative value if you haven't done with the next. We could look next. Then what is maximum? The maximum value is minus one, so here let's draw minus one Sure. We are not Getting the value, we multiply our current value with minimum value we get from this value itself, this value is channel. To handle negative value, this max value isn't helpful. To handle negative value, this minimum value is helpful. Now here we get minus fifty and minus one, so including minus one, so including minus one, we get minimum below minus fifty and maximum below minus one. You see, minus one and fifty is fifty. Let's move forward. Our next, our next element is Our next element is minus two. So let's compute minimum for this cell. So minimum equals to minus two, then minus two times fifty, which is equals to thirty, then minus two, minus two times minus one is two, so minimum is minus two here minimum is minus two. Now let's compute max(max equals to minus two then thirty then two). Max is thirty, so we get thirty. On the next we have no values, this is our last value. Here is the max equals to thirty. So when you're going to take the max of answer and max, so we get thirty. So answer equals to thirty. We solved this problem successfully, and we find out this answer thirty. So we encountered one important case, if we encounter zero, what do we need to do? We reset our min and max to one. This is our algorithm. This algorithm takes time complexity O of n. This algorithm takes time complexity O of n, n is the length of the given array, and this algorithm takes space complexity O of 1, which is constant. This is our algorithm. I hope you understood this algorithm on a very high level.


