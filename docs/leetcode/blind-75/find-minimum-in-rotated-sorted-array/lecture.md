# Find Minimum in Rotated Sorted Array

```python

# 1st array example
arr = [6,7,8,9,0,1,2,3,4,5]

# 2nd array example
arr = [3,4,5,6,1]
```


So in this video, we're going to solve this coding interview question, find minimum in a rotated sorted array. In this problem, we're given an array of integers, and the array is rotated. This is an array. If we rotate this array Six times, then we'll get this area. So if we rotate this area six times, this zero will move right here. Okay, let's draw this. So zero will move here, then this one will move here, then this two will move here, let's draw here, this two will move here, then this three, then this three will move here, then this four will move here, then this five will move here. So we'll get this array. This array is rotated six times. We rotated this array six times, we'll get this array. Now, how to solve this problem? If we're given this array, one, two, three, four, if we If we're given this array, if we riff it, if we're given this array 1, 2, 3, 4, if we rotate this array one time, then we'll get 2, 3, 4, 1. If we rotate this array two times, we'll get here. Now we have to solve this problem. We can solve this problem pretty easily by scanning this array from left to right and keep tracking the minimum element, but that will take us O(n) time complexity. We have to solve this problem in logarithmic time complexity. When we solve, we have to solve this problem in logarithmic time complexity. The common algorithm we use binary search algorithm. We can't apply binary search algorithm directly because the array isn't completely sorted. Binary search algorithm works for sorted array. But this array is rotated sorted array. We can apply binary search algorithm by modify, so we have to use modified binary search algorithm. Let's see how to solve this problem using modified binary search algorithm. Let's assume we're given this array of integers. First, at us, left point to the first element and right point to the last element. What we're going to do now, we're gonna find the middle. This is the middle. We can find the middle simply applying this formula, left plus right divided by two. First, let's talk about the base case of this problem. If we're given an array of length one, then we have to return the element from the array. We have only one element. If we're given an array of length two We'll return the minimum of two We can do this in constant time, and if we're given an array completely sorted, if we're given an array that is completely sorted, we have to return the first element. We can compare that by comparing the first element and the last element. If the first element is less than the last element, it means the array is completely sorted. Now, let's see how to solve this problem for this type of inputs. This type of inputs will apply modified binary search. Let's see how. So we have left pointer, right pointer, and middle pointer. Now our goal is to find out the decreasing part in this array. The decreasing part. First, we're gonna try to find the decreasing part at middle plus one, and then we'll check middle. Now, what I'm gonna do, I'm gonna check, does the array is decreasing at middle plus one? We see no. So let's check for middle. Does this array is decreasing at middle? Yes, it is. This array is decreasing here. So we find the decreasing element. The decreasing element is our answer. The decreasing element is minimum. If we find a decreasing point, it means that we find the minimum. So here we see this array is decreasing. So this is our answer. Let's assume that we don't have this part here. We don't have this part for this area. We have this array. So left will point here and right will point here. Let's calculate the middle, the middle will point right here. Now we see that the Array is not decreasing at middle plus one and the array is not decreasing at middle. So what I'm gonna do, I'm gonna find out the sorted portion. We'll have array is sorted from left to middle or from middle to right. We see that this array is sorted for this, left to middle. So we'll discard this part. Let's discard it. Let's move left to middle plus one. So left will point here. Now let's find out the middle. Middle is zero. Now let's find out the decreasing part. Does the array is decreasing at middle plus one? Right here, no. Then let's check does the array is decreasing at middle. Yes, it is. So we find out the decreasing point. This is the minimum. The minimum is located at decreasing point. So this is decreasing point. Here we have the minimum. So we'll write in zero. We'll first compare middle plus one, then middle. First, we'll check does the array is decreasing at middle plus one, then we'll check at middle. That's because we might have an array something like this of length two. We'll find out this summary by discarding left and right portions. So here we'll see left will point here and middle will point here and right will point here. If we first compare this middle, then we will have index out of bound. We have to compare this value with the immediate left value. There is no immediate left value. If we have two elements Then we will have always the decreasing position right here, not here, okay? We'll have always this right here, because the array is rotated and sorted. We'll have something like this two, one. We'll never have something like this one, two. When we have left and middle points to the same, this is our algorithm to solve this problem. Let's take another example. If we're given this array, let's see how to solve it. Left will point here, right will point here. Let's find a middle. This is our middle. We see the array is not decreasing here. This array is not decreasing here. So the array is not decreasing at middle plus one, and the array is not decreasing at middle. Now let's find out the sorted portion. This is sorted portion. Let's discard it. So let's move left right here. Now let's find out the middle. This is the middle. Now we're gonna check, does this array is decreasing at middle plus one? Right here. Yes, it is. So this is your answer. Let's take another example. For example, if we're given this array, left will point here, right will point here. Let's find out the middle. The middle is This middle point here, does this area decreasing at middle plus one right here? No. Does this area decreasing at middle? No. So let's find out the sorted portion or the increasing part. This is the increasing part, so let's discard this part. So right point here, let's find out middle. This is the middle. Now we're gonna check, does the area decreasing at middle plus one? Yes, it is. It will decrease right here. So this is your answer. This is how we can solve this problem. This algorithm will takes log of in time complexity. So the runtime complexity of this algorithm is logarithmic, and it will takes constant space complexity because we're not using any additional data structure. If I find out an array of length two by discarding some part of it, we'll have only this answer on the middle plus one, because the array is rotated and sorted. This is the algorithm to solve this problem efficiently. Now, let's implement this algorithm. Now let's implement. no sound.

